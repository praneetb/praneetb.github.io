# Public travel data. Keep this list empty.
# Do not invent entries. Do not commit a private visited-country list here.
# Public /travel/ reads this file only, so the globe stays empty until a real public list exists.
# Visited countries live in the signed-in browser's localStorage.

countries: []
